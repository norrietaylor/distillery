# T02.2 Proof Summary

Task: Add --project flag to /classify, /minutes, /radar SKILL.md

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T02.2-01-file.txt | file grep count | PASS |
| T02.2-02-file.txt | file grep count | PASS |
| T02.2-03-file.txt | file grep count | PASS |

## Changes Made

### .claude-plugin/skills/classify/SKILL.md
- Added `--project <name>` flag to the optional flags table in Step 2
- Mode B (Step B1): added `project=<name>` parameter note to `distillery_list` call
- Mode C (Step C1): added `project=<name>` parameter note to `distillery_review_queue` call

### .claude-plugin/skills/minutes/SKILL.md
- Added `--project <name>` to optional flags table in Step 2 (Parse Mode)
- List Mode (Step 3c): added `project=<name>` parameter note to `distillery_list` call

### .claude-plugin/skills/radar/SKILL.md
- Added `--project <name>` to flags table in Step 2 (Parse Arguments)
- Step 3: added `project=<name>` parameter note to `distillery_list` call

## Verification

All three skill files now contain at least one `--project` reference (classify: 3, minutes: 2, radar: 2).
The flag follows the same pattern established in /recall and /pour SKILL.md files.
