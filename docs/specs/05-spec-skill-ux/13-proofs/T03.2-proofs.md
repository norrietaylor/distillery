# T03.2 Proof Summary: Implement distillery_configure MCP tool

## Task
Implement `distillery_configure` MCP tool that accepts runtime configuration
changes, validates them, applies to in-memory config, and writes atomically
to `distillery.yaml` on disk.

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | test | T03.2-01-test.txt | PASS |
| 2 | cli  | T03.2-02-cli.txt  | PASS |
| 3 | file | T03.2-03-file.txt | PASS |

## Test Coverage (22 tests)
- Required parameter validation (section, key, value)
- Unknown key rejection via allowlist
- Range validation (0.0-1.0 for thresholds, 1-100 for limits)
- Type coercion (string to float/int)
- Cross-field constraints (alert >= digest)
- In-memory config update and revert on failure
- Atomic disk write via temp-file + rename
- No-op when value is unchanged
- Graceful handling when no config file exists (in-memory only)

## Implementation Files
- `src/distillery/mcp/tools/configure.py` -- handler module
- `src/distillery/mcp/tools/__init__.py` -- export added
- `src/distillery/mcp/server.py` -- tool registered (23 tools total)
- `tests/test_mcp_configure.py` -- 22 unit tests
- `tests/test_mcp_server.py` -- tool registration test updated

## Configurable Keys (allowlist)
- `feeds.thresholds.alert` (float, 0.0-1.0, must be >= digest)
- `feeds.thresholds.digest` (float, 0.0-1.0, must be <= alert)
- `defaults.dedup_threshold` (float, 0.0-1.0)
- `defaults.dedup_limit` (int, 1-100)
- `defaults.stale_days` (int, 1-3650)
- `classification.confidence_threshold` (float, 0.0-1.0)
