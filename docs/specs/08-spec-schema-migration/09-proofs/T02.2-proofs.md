# T02.2 Proof Summary: Implement run_pending_migrations and get_current_schema_version

## Task

Add `get_current_schema_version(conn)` and `run_pending_migrations(conn, *, dimensions, vss_available)` to `src/distillery/store/migrations.py`.

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | test | T02.2-01-test.txt | PASS |
| 2 | cli  | T02.2-02-cli.txt  | PASS |
| 3 | cli  | T02.2-03-cli.txt  | PASS |

## Details

1. **Smoke tests** (T02.2-01-test.txt): 6 inline tests covering fresh DB (version 0), full migration (0 to 6), version persistence, idempotent re-run, partial migration (3 to 6), and failed migration error handling.
2. **mypy --strict** (T02.2-02-cli.txt): No type errors found.
3. **ruff check** (T02.2-03-cli.txt): All lint checks passed.

## Implementation Notes

- `get_current_schema_version` handles missing `_meta` table via `CatalogException` catch, returning 0.
- `run_pending_migrations` runs each migration in its own transaction with rollback on failure.
- After all pending migrations succeed, `schema_version` is upserted in `_meta`.
- Uses `contextlib.suppress(Exception)` for rollback safety per ruff SIM105.
