Type: cli
Command: python3 -c "from distillery.cli import _build_parser; p = _build_parser(); p.parse_args(['import', '--help'])"
Expected: import subcommand appears with --input, --mode, --yes flags
Timestamp: 2026-04-03T00:00:00Z

usage: distillery import [-h] [--config PATH] [--format {text,json}]
                         --input PATH [--mode {merge,replace}] [--yes]

options:
  -h, --help            show this help message and exit
  --config PATH         Path to configuration file (overrides
                        DISTILLERY_CONFIG env var and the default
                        distillery.yaml search)
  --format {text,json}  Output format (default: text)
  --input PATH          Path to the JSON export file to import
  --mode {merge,replace}
                        Import mode: merge (skip existing IDs) or replace
                        (delete all before import)
  --yes                 Skip confirmation prompt for replace mode

Status: PASS
