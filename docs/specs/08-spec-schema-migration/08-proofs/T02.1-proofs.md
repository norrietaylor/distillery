# T02.1 Proof Summary: Create migrations.py with extracted migration functions

## Task
Create `src/distillery/store/migrations.py` containing the `MIGRATIONS` dict and 6 extracted migration functions.

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | cli (mypy) | T02.1-01-mypy.txt | PASS |
| 2 | cli (import) | T02.1-02-import.txt | PASS |
| 3 | test (regression) | T02.1-03-regression.txt | PASS |

## Summary

- `migrations.py` created with 6 migration functions and `MIGRATIONS` registry
- All functions accept `conn: duckdb.DuckDBPyConnection` + `**kwargs: Any`
- `MigrationFunc` Protocol defined for type safety
- SQL constants moved from `duckdb.py` (duplicated for now; T02.3 will refactor imports)
- mypy --strict passes with no issues
- All 76 existing store tests pass (no regressions)
- ruff lint clean
