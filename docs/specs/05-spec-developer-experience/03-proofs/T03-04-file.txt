Type: file
Description: CI workflow updated with matrix strategy, pip caching, coverage enforcement, and artifact upload
Expected: .github/workflows/ci.yml contains matrix for 3.11/3.12/3.13, cache: "pip", --cov-fail-under=80, upload-artifact step
Timestamp: 2026-03-25T00:00:00Z

--- FILE DIFF SUMMARY ---
File: .github/workflows/ci.yml

Changes from single-version to matrix:
  + strategy:
  +   fail-fast: false
  +   matrix:
  +     python-version: ["3.11", "3.12", "3.13"]

Added pip caching:
  + cache: "pip"

Replaced plain pytest run with coverage-enabled run:
  - pytest
  + pytest --cov=src/distillery --cov-report=xml --cov-report=term-missing --cov-fail-under=80

Added coverage artifact upload step:
  + - name: Upload coverage report
  +   if: always()
  +   uses: actions/upload-artifact@v4
  +   with:
  +     name: coverage-${{ matrix.python-version }}
  +     path: coverage.xml
  +     retention-days: 14

Status: PASS
