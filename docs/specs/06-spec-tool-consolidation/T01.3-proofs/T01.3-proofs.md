# T01.3 Proof Summary

**Task:** Update webhooks.py, all 10 SKILL.md files, and CONVENTIONS.md

## Changes Made

### webhooks.py
- Removed imports of `_handle_quality` and `_handle_suggest_sources`
- Replaced `_handle_quality(store=store, arguments={})` with `_handle_metrics(..., arguments={"scope": "search_quality"})`
- Replaced `_handle_suggest_sources(store=store, config=config, arguments={"max_suggestions": 3})` with `_handle_interests(store=store, config=config, arguments={"suggest_sources": True, "max_suggestions": 3})`

### All 10 SKILL.md files
Replaced `"mcp__*__distillery_status"` with `"mcp__*__distillery_metrics"` in allowed-tools frontmatter:
- distill/SKILL.md
- recall/SKILL.md
- pour/SKILL.md
- bookmark/SKILL.md
- minutes/SKILL.md
- classify/SKILL.md
- watch/SKILL.md
- radar/SKILL.md (also replaced `distillery_suggest_sources` with `distillery_interests`)
- setup/SKILL.md (also updated body text references)
- tune/SKILL.md (also updated body text references)

### CONVENTIONS.md
- Updated MCP Health Check section: `distillery_status` → `distillery_metrics(scope="summary")`
- Updated Skills Registry: `/tune` primary tool `distillery_status` → `distillery_metrics`
- Updated Skills Registry: `/radar` primary tool `distillery_suggest_sources` → `distillery_interests`

### README.md (skills/)
- Updated MCP Tools list: `distillery_status` → `distillery_metrics`
- Updated debugging hint to use `distillery_metrics(scope="summary")`

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T01.3-01-grep.txt | grep search for removed refs | PASS |
| T01.3-02-lint.txt | ruff lint on webhooks.py | PASS |
| T01.3-03-webhooks.txt | webhooks.py import/call verification | PASS |
