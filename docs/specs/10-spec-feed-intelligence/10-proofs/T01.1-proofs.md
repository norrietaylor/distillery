# T01.1 Proof Summary

**Task**: T01.1 — Add get_tag_vocabulary() to store protocol and DuckDBStore
**Status**: COMPLETED
**Date**: 2026-04-06

## Artifacts

| File | Type | Status |
|------|------|--------|
| T01.1-01-test.txt | test | PASS |

## Summary

All 6 new integration tests for `get_tag_vocabulary` pass:

- `test_tag_vocabulary_empty_store` — empty store returns `{}`
- `test_tag_vocabulary_returns_correct_counts` — correct per-tag counts across entries
- `test_tag_vocabulary_excludes_archived` — archived entries excluded
- `test_tag_vocabulary_prefix_filtering` — prefix filters by exact match or `{prefix}/` prefix
- `test_tag_vocabulary_prefix_exact_match` — tag equal to prefix is included
- `test_tag_vocabulary_prefix_no_partial_word_match` — partial word match does not match (e.g. `py` does not match `python`)

Full test suite: 31 passed (25 pre-existing + 6 new).

## Implementation Notes

- Added `get_tag_vocabulary(prefix: str | None = None) -> dict[str, int]` to `DistilleryStore` protocol in `store/protocol.py`
- Implemented `_sync_get_tag_vocabulary` + `get_tag_vocabulary` in `DuckDBStore` in `store/duckdb.py`
- Uses the same SQL pattern as `_handle_tag_tree`: queries `tags` from `entries WHERE status != 'archived'`, iterates over tag arrays in Python
- Wraps sync DuckDB call with `asyncio.to_thread`
- ruff and mypy --strict both pass
