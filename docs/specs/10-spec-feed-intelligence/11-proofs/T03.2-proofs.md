# T03.2 Proof Summary — Schema Migration 7: FTS Extension and Index

**Task**: T03.2 (native ID #11)
**Subject**: Schema migration 7 — FTS extension and index
**Status**: COMPLETED
**Timestamp**: 2026-04-06T00:00:00Z

## Artifacts

| File | Type | Status |
|------|------|--------|
| T03.2-01-test.txt | test | PASS |

## Implementation Summary

Added migration 7 to `src/distillery/store/migrations.py`:

- Function `create_fts_index(conn, **kwargs)` installs the DuckDB FTS extension
  and creates a full-text index on `entries.content` using
  `PRAGMA create_fts_index('entries', 'id', 'content', overwrite=1)`.
- `overwrite=1` makes the migration idempotent.
- Gracefully degrades on `duckdb.IOException` (e.g., offline install) or any
  other exception — logs a warning and sets `kwargs["fts_available"] = False`.
- Registered as entry `7` in the `MIGRATIONS` dict.

## Test Results

Command: `pytest tests/test_store_integration.py -k 'fts or migration_7' -v`

- `test_migration_7_is_registered` — PASS
- `test_migration_7_runs_without_error` — PASS
- `test_migration_7_is_idempotent` — PASS
- `test_migration_7_fts_search_functional` — PASS

All 4 tests passed. No regressions in existing integration tests.
