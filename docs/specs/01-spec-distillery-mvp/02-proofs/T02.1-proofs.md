# T02.1 Proof Summary: Define DistilleryStore Protocol and SearchResult

## Task

Define the `DistilleryStore` protocol in `src/distillery/store/protocol.py` as a Python
`Protocol` class with async methods, and export from `store/__init__.py`.

## Files Created/Modified

- `src/distillery/store/protocol.py` -- full implementation
- `src/distillery/store/__init__.py` -- exports `DistilleryStore` and `SearchResult`

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T02.1-01-cli.txt | Protocol import + isinstance check | PASS |
| T02.1-02-cli.txt | Method signature + docstring audit | PASS |
| T02.1-03-cli.txt | ruff lint check | PASS |

## Summary of Verification

### Protocol Methods (all 7 required by spec)

| Method | Async | Type Hints | Docstring |
|--------|-------|-----------|-----------|
| `store(entry) -> str` | yes | yes | yes |
| `get(entry_id) -> Entry \| None` | yes | yes | yes |
| `update(entry_id, updates) -> Entry` | yes | yes | yes |
| `delete(entry_id) -> bool` | yes | yes | yes |
| `search(query, filters, limit) -> list[SearchResult]` | yes | yes | yes |
| `find_similar(content, threshold, limit) -> list[SearchResult]` | yes | yes | yes |
| `list_entries(filters, limit, offset) -> list[Entry]` | yes | yes | yes |

### SearchResult Dataclass

- `entry: Entry` field present
- `score: float` field present

### Behavioural Contracts in Docstrings

- `update()` documents that `id`, `created_at`, `source` are immutable (raises `ValueError`)
- `delete()` documents soft-delete via setting status to `archived`
- `search()` documents all supported filter keys
- `find_similar()` documents threshold semantics

### Lint

`ruff check` passes with zero errors on both files.

### Structural Subtyping

`isinstance(MockStore(), DistilleryStore)` returns `True` for a class that implements
all 7 async methods (verified via `@runtime_checkable`).

## Overall Result: PASS
