Type: cli
Command: python -c "import asyncio; from distillery.store.duckdb import DuckDBStore; ..."
Expected: DuckDBStore initializes in-memory, creates entries table with all columns, HNSW index present
Timestamp: 2026-03-21T00:00:00Z

--- OUTPUT ---

Initialized OK
  id: VARCHAR
  content: VARCHAR
  entry_type: VARCHAR
  source: VARCHAR
  author: VARCHAR
  project: VARCHAR
  tags: VARCHAR[]
  status: VARCHAR
  metadata: JSON
  created_at: TIMESTAMP
  updated_at: TIMESTAMP
  version: INTEGER
  embedding: FLOAT[1024]
Indexes: [('idx_entries_embedding',)]
Re-initialize (no-op) OK
Closed OK

--- ANALYSIS ---

All 12 Entry-related columns present in the entries table.
Embedding column uses FLOAT[1024] matching the mock provider's dimensions.
HNSW index idx_entries_embedding created successfully.
Re-initialization is idempotent (no-op on second call).
Connection close works cleanly.

Status: PASS
