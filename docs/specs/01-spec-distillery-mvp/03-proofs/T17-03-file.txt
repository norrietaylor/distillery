Type: file
Command: grep -n "embed\|_meta\|_validate_or_record_meta\|_create_meta_table" src/distillery/store/duckdb.py
Expected: Implementation references confirm embedding integration and _meta table
Timestamp: 2026-03-22T00:00:00Z
Status: PASS

Key implementation points in src/distillery/store/duckdb.py:

1. _CREATE_META_TABLE SQL (line 63-68):
   - Creates _meta table with key VARCHAR PRIMARY KEY and value VARCHAR NOT NULL

2. _create_meta_table() method (line 157-159):
   - Called during _sync_initialize to create the _meta table

3. _validate_or_record_meta() method (line 161-212):
   - On first use: records embedding_model and embedding_dimensions in _meta
   - On subsequent opens: compares stored model/dims with configured provider
   - Raises RuntimeError on model name mismatch
   - Raises RuntimeError on dimensions mismatch

4. _sync_initialize() (line 214-224):
   - Calls _create_meta_table() and _validate_or_record_meta() during init

5. _sync_store() (line 276-304):
   - Calls self._embedding_provider.embed(entry.content) for real embeddings
   - Replaces previous placeholder zero-vector approach

6. _sync_update() (line 373-377):
   - When "content" is in updates, calls self._embedding_provider.embed()
   - Re-embeds content on update to keep embeddings consistent

7. search() (line 577):
   - Calls self._embedding_provider.embed(query) to embed query text

8. find_similar() (line 628):
   - Calls self._embedding_provider.embed(content) to embed content text
