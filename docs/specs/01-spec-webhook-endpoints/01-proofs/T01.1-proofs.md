# T01.1 Proof Artifacts: Add WebhookConfig dataclass and wire into ServerConfig + _parse_server()

## Summary

Successfully implemented WebhookConfig dataclass in `src/distillery/config.py` with the following changes:

1. Added `WebhookConfig` dataclass with `enabled: bool = True` and `secret_env: str = "DISTILLERY_WEBHOOK_SECRET"`
2. Wired `WebhookConfig` into `ServerConfig` as a new field `webhooks: WebhookConfig`
3. Updated `_parse_server()` function to parse the `webhooks` section from YAML config

All changes follow existing code patterns and conventions:
- Dataclass with proper docstrings and Attributes section
- Full type annotations with mypy --strict compliance
- Proper YAML parsing with error handling
- Comprehensive test coverage

## Implementation Details

### Files Modified
- `src/distillery/config.py`: Added WebhookConfig dataclass and integration
- `tests/test_config.py`: Added 10 new tests for WebhookConfig

### Changes Made

1. **WebhookConfig dataclass** (lines 248-258):
   - `enabled: bool = True` - Controls whether webhooks are enabled
   - `secret_env: str = "DISTILLERY_WEBHOOK_SECRET"` - Environment variable name for the webhook secret

2. **ServerConfig integration** (lines 260-273):
   - Added `webhooks: WebhookConfig = field(default_factory=WebhookConfig)`
   - Updated docstring to document the new field

3. **_parse_server() update** (lines 658-726):
   - Added parsing for the `webhooks` section from YAML config
   - Handles null/missing webhooks section (defaults provided)
   - Validates webhooks section is a mapping (dict), not a list
   - Passes parsed values to WebhookConfig constructor

## Test Coverage

### TestWebhookConfigDefaults (4 tests)
- test_webhook_config_dataclass_defaults: Verify default values
- test_webhook_config_enabled_true: Verify enabled=True works
- test_webhook_config_enabled_false: Verify enabled=False works
- test_webhook_config_custom_secret_env: Verify custom secret_env works

### TestWebhookConfigParsing (6 tests)
- test_webhooks_defaults_when_section_absent: YAML parsing defaults
- test_webhooks_enabled_true: Parse enabled=true
- test_webhooks_enabled_false: Parse enabled=false
- test_webhooks_custom_secret_env: Parse custom secret_env
- test_webhooks_all_fields: Parse all fields together
- test_server_webhooks_as_list_raises: Validation of mapping type

All 87 config tests pass (77 existing + 10 new).

## Quality Checks

### Type Safety
- mypy --strict: ✓ PASS (no issues)
- Strict type annotations throughout
- Proper use of dataclass fields with defaults

### Code Style
- ruff check: ✓ PASS (all rules satisfied)
- ruff format: ✓ PASS (properly formatted)
- Line length: 100 characters max
- Follows existing code patterns

## Proof Artifacts Generated

1. **T01.1-01-test-webhook-config-defaults.txt**: TestWebhookConfigDefaults test run
2. **T01.1-02-test-webhook-config-parsing.txt**: TestWebhookConfigParsing test run
3. **T01.1-03-test-all-config.txt**: Full config test suite (87 tests)
4. **T01.1-04-mypy-check.txt**: mypy strict type checking
5. **T01.1-05-ruff-check.txt**: ruff linting and formatting

## Next Steps

This task completes the configuration layer for webhook infrastructure. The next tasks will implement:
- T01.2: Create `src/distillery/mcp/webhooks.py` with auth and cooldown logic
- T01.3: Wire webhooks into the FastMCP server and __main__.py
- T02: Implement webhook endpoint handlers (poll, rescore, maintenance)
- T03: Deploy config and GitHub Actions workflow
