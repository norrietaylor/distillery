# T01.3 Proof Summary: Update distillery status to show version info + tests

**Task**: T01.3 — Update `distillery status` to show `schema_version` and `duckdb_version` from `_meta`
**Date**: 2026-04-03
**Status**: PASS

## Changes Made

### `src/distillery/cli.py`
- `_query_status()`: reads `schema_version` and `duckdb_version` from the `_meta` table when it exists; returns `None` for both when the table is absent (uninitialised DB)
- `_cmd_status()`: includes `schema_version` and `duckdb_version` in the returned `data` dict; text format prints them when non-None; JSON format always includes both keys

### `tests/test_cli.py`
Added to `TestQueryStatus`:
- `test_returns_expected_keys`: asserts `schema_version` and `duckdb_version` present in result dict
- `test_version_keys_none_on_uninitialised_db`: asserts both are `None` for a raw `:memory:` DB

Added to `TestCmdStatusUnit`:
- `test_cmd_status_json_includes_version_keys`: JSON output contains both version keys
- `test_cmd_status_text_shows_version_when_available`: initialises a real DuckDBStore, then calls `_cmd_status`; asserts `schema_version:` and `duckdb_version:` appear in text output with the current duckdb version

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T01.3-01-test.txt | test | PASS |
| T01.3-02-test.txt | test | PASS |

## T01.3-01-test.txt

Tests: `TestQueryStatus` and `TestCmdStatusUnit` (all new version-info tests).

Result: **PASS** — all 9 tests in the two classes pass.

## T01.3-02-test.txt

Tests: `tests/test_store_migration.py` (T01.2 coverage — `_meta` version tracking).

Result: **PASS** — 3 tests pass, confirming the `_meta` table backing these CLI tests is correct.
