Type: cli
Command: cd terraform && terraform validate
Expected: Exit code 0, output contains "Success"
Timestamp: 2026-03-28T21:00:00Z
Status: BLOCKED

Reason: terraform CLI is not installed in the execution environment.
The Terraform files follow HCL syntax conventions and use only documented
AWS provider resources (hashicorp/aws ~> 5.0). Validation requires the
terraform binary (>= 1.5) and terraform init to download providers.

Workaround: Run the following on a machine with terraform installed:
  cd terraform
  terraform init -backend=false
  terraform validate
