# T04.3 Proof Summary: Write feeds handler tests (test_mcp_feeds.py)

## Task

T04.3 — Write feeds handler tests for the three MCP feed tool handlers extracted
to `src/distillery/mcp/tools/feeds.py`.

## Proof Artifacts

| Artifact | Type | Status |
|---|---|---|
| T04.3-01-test.txt | cli (pytest) | PASS |

## Results

- **41 tests created** in `tests/test_mcp_feeds.py`
- **41/41 tests pass** (0 failures, 0 errors)

## Handlers Covered

### _handle_watch (24 tests)
- List action: empty sources, populated sources, field shape, backend error
- Add action: rss/github success, default/custom poll_interval, custom trust_weight, missing url, missing source_type, invalid source_type, zero poll_interval, out-of-range trust_weight, duplicate URL, response includes updated sources list
- Remove action: existing source, nonexistent source, missing url, selective removal, response includes updated sources
- Invalid action: invalid action string, missing action, non-string action

### _handle_poll (6 tests)
- Success: full summary returned with correct fields
- Source URL filter: poll called with specific source_url, verified via mock assert
- Unknown source_url: NOT_FOUND error returned without calling poller
- Poll error: RuntimeError from FeedPoller returns POLL_ERROR with message
- ISO datetime: polled_at field parses as ISO-format datetime
- No sources: empty summary returned correctly

### _handle_suggest_sources (11 tests)
- GitHub suggestions from tracked_repos
- RSS suggestions from bookmark_domains
- Empty store returns no suggestions
- Already-watched sources excluded from results
- Custom max_suggestions limits count
- source_types filter (github only)
- Invalid source_types returns INVALID_SOURCE_TYPE
- Zero max_suggestions returns validation error
- Negative recency_days returns validation error
- Extraction error returns EXTRACTION_ERROR with message
- Response always has required shape keys

## Commit

Created: `tests/test_mcp_feeds.py`
Proof dir: `docs/specs/05-spec-mcp-refactor/04-proofs/`
