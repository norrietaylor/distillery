# T01.7 Proof Summary: Extract feeds handlers to tools/feeds.py

**Task**: T01.7 — Extract feeds handlers to tools/feeds.py
**Date**: 2026-04-01
**Status**: PASS

## What Was Done

Moved `_handle_watch`, `_handle_poll`, `_handle_rescore`, and `_handle_suggest_sources`
from `server.py` into a new `src/distillery/mcp/tools/feeds.py` module.

Also moved related helpers: `_poll_result_to_dict`, `_normalise_watched_set`,
`_derive_suggestions`, `_VALID_SOURCE_TYPES`, `_VALID_SUGGEST_SOURCE_TYPES`.

`_handle_rescore` was extracted from inline code inside the `distillery_rescore`
tool decorator in server.py — it previously had no standalone handler function.
It now accepts `store`, `config`, and `arguments` dict, consistent with the other
extracted handlers.

### Files Created
- `src/distillery/mcp/tools/feeds.py` — all 4 handlers + helpers

### Files Modified
- `src/distillery/mcp/server.py` — removed inline handlers, added import from feeds.py,
  updated `distillery_rescore` tool decorator to delegate to `_handle_rescore`
- `src/distillery/mcp/tools/__init__.py` — added exports for all 4 feeds handlers

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T01.7-01-test.txt | test | PASS |

## Test Coverage

- `tests/test_watch.py` — 35 unit tests for `_handle_watch` (list/add/remove actions)
- `tests/test_poller.py` — 28 unit tests for `_handle_poll` (success and error paths)
- `tests/test_interests.py` — 43 unit tests for `_handle_suggest_sources`

All 895 unit tests pass (2 skipped, 1 warning, none related to feeds).

## Requirement Verification

| Requirement | Status |
|-------------|--------|
| R01.7.1: feeds.py contains all 4 handlers | PASS |
