# T01.5 Proof Summary: Extract quality handlers to tools/quality.py

## Task

T01.5 — Extract `_handle_check_dedup` and `_handle_check_conflicts` from `server.py` to `src/distillery/mcp/tools/quality.py`.

## Requirement

R01.5.1: `quality.py` contains `_handle_check_dedup`, `_handle_check_conflicts`

## Artifacts

| File | Type | Status |
|------|------|--------|
| T01.5-01-test.txt | test | PASS |

## Summary

- Created `src/distillery/mcp/tools/quality.py` with both handlers extracted verbatim from `server.py`.
- Updated `src/distillery/mcp/tools/__init__.py` to export both handlers.
- Updated `src/distillery/mcp/server.py` to import handlers from `tools.quality` and removed duplicate implementations.
- Removed now-unused `EmbeddingBudgetError` and `record_and_check` imports from `server.py` (they are used in `quality.py` directly).
- All 9 tests in `tests/test_mcp_dedup.py` pass (import path `distillery.mcp.server._handle_check_dedup` still works via re-export).
