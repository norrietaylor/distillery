# T01.2 Proof Summary

Task: Store protocol + DuckDB implementation for relations

## Artifacts

| File | Type | Status |
|------|------|--------|
| T01.2-01-test.txt | test | PASS |
| T01.2-02-typecheck.txt | cli | PASS |

## Summary

- Added `add_relation`, `get_related`, `remove_relation` to `DistilleryStore` protocol in `src/distillery/store/protocol.py`
- Implemented `_sync_add_relation`, `_sync_get_related`, `_sync_remove_relation` and their async wrappers in `src/distillery/store/duckdb.py`
- `add_relation` validates both entry IDs exist (raises `ValueError` if not) and returns a UUID relation ID
- `get_related` supports `direction` param (`outgoing`/`incoming`/`both`) and optional `relation_type` filter
- `remove_relation` returns `bool` indicating whether the row was found and deleted
- All DuckDB methods use `asyncio.to_thread` following established patterns
- 16 unit tests written in `tests/test_relations.py`, all passing
- mypy strict: no issues
- ruff: no issues
