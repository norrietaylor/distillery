Type: file
Command: grep -E 'image|workflow_run' .github/workflows/fly-deploy.yml
Expected: Contains --image ghcr.io/norrietaylor/distillery and workflow_run trigger
Timestamp: 2026-03-30T00:00:00Z

Output:
  workflow_run:
      github.event.workflow_run.conclusion == 'success'
      - name: Deploy from GHCR image
          SHORT_SHA="${{ github.event.workflow_run.head_sha || github.sha }}"
            --image "ghcr.io/norrietaylor/distillery:sha-${SHORT_SHA}"

Status: PASS
