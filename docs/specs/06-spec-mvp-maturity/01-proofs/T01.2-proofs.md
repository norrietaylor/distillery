# T01.2 Proof Summary

**Task**: Add log_search() and log_feedback() to store protocol and DuckDB

**Status**: PASS

## Changes Made

### src/distillery/store/protocol.py
- Added `log_search(query, result_entry_ids, result_scores, session_id=None) -> str` to `DistilleryStore` protocol
- Added `log_feedback(search_id, entry_id, signal) -> str` to `DistilleryStore` protocol
- Both methods are fully documented with Args/Returns docstrings

### src/distillery/store/duckdb.py
- Added `_sync_log_search()` private method: generates UUID, INSERTs into `search_log`, returns UUID string
- Added `log_search()` async public method: wraps `_sync_log_search` with `asyncio.to_thread`
- Added `_sync_log_feedback()` private method: generates UUID, INSERTs into `feedback_log`, returns UUID string
- Added `log_feedback()` async public method: wraps `_sync_log_feedback` with `asyncio.to_thread`
- All four methods follow the exact pattern established by `_sync_store` / `store()`

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T01.2-01-test.txt | pytest (103 store tests) | PASS |
| T01.2-02-cli.txt | Integration assertions | PASS |

## Notes

- The `test_create_server_registers_all_tools` and `test_server_registers_all_twelve_tools` failures are pre-existing and caused by T03.2 (worker-5) adding `distillery_check_conflicts` while the tests check an exact tool set — not caused by this task.
- All store protocol, DuckDB store, and store integration tests (103) pass cleanly.
- The `uuid` import is inside the sync methods to avoid polluting the module-level namespace (pattern used elsewhere in Python stdlib utilities).
