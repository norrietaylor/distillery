# T01.1 Proof Summary

Task: Add search_log and feedback_log tables to DuckDB schema

## Changes Made

### src/distillery/store/duckdb.py
- Added `_CREATE_SEARCH_LOG_TABLE` SQL constant (id, query, result_entry_ids VARCHAR[], result_scores FLOAT[], timestamp, session_id)
- Added `_CREATE_FEEDBACK_LOG_TABLE` SQL constant (id, search_id FK to search_log.id, entry_id, signal, timestamp)
- Added `_create_log_tables()` method to `DuckDBStore`
- Called `_create_log_tables()` in `_sync_initialize()` after `_create_schema()` (entries table created first)

### src/distillery/config.py
- Added three new fields to `ClassificationConfig`:
  - `feedback_window_minutes: int = 5`
  - `stale_days: int = 30`
  - `conflict_threshold: float = 0.60`
- Updated `_parse_classification()` to parse all three fields from YAML
- Updated `_validate()` to validate all three fields (positive int checks, float [0,1] range)

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T01.1-01-test.txt | test | PASS |
| T01.1-02-cli.txt | cli | PASS |

## Verification

- 413 tests pass (full suite minus E2E)
- DuckDB in-memory store creates both new tables on initialize()
- search_log has correct column types including VARCHAR[] and FLOAT[] arrays
- feedback_log has FK reference to search_log.id
- ClassificationConfig defaults match spec: feedback_window_minutes=5, stale_days=30, conflict_threshold=0.60
- load_config() correctly returns defaults without a YAML file
