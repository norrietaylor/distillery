# T03.1 Proof Summary: Implement heuristic classifier with centroid computation

## Task
Implement `HeuristicClassifier` in `src/distillery/classification/heuristic.py` with centroid computation and cosine similarity classification.

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | test | T03.1-01-test.txt | PASS (19/19 tests) |
| 2 | mypy | T03.1-02-mypy.txt | PASS (strict, no errors) |
| 3 | lint | T03.1-03-lint.txt | PASS (ruff clean) |

## Implementation Summary

- Created `src/distillery/classification/heuristic.py` with `HeuristicClassifier` class
- Class provides `classify()`, `compute_centroids()`, and `classify_entry()` methods
- Centroid computation averages embeddings per type, L2-normalises, excludes types with < 3 entries
- Classification uses cosine similarity with 0.5 threshold; below threshold sends to pending_review
- Follows Protocol-based design pattern (no ABCs), full mypy --strict compliance
- Updated `classification/__init__.py` to export `HeuristicClassifier`
- Created `tests/test_classification/test_heuristic.py` with 19 unit/integration tests

## Test Coverage

- `TestCosineSimilarity`: 4 tests (identical, orthogonal, opposite, zero vectors)
- `TestComputeCentroids`: 6 tests (sufficient entries, insufficient, inbox excluded, normalised, empty, multiple types)
- `TestClassifyEntry`: 4 tests (above threshold, below threshold, exact match, best-of-multiple)
- `TestClassifyIntegration`: 5 tests (matching centroid, no match, no centroids, insufficient data, return type)

## Completed
2026-04-09
