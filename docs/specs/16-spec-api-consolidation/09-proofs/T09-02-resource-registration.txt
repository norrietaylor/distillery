Type: cli
Command: python3 -c "...; resources = await server.list_resources(); result = await server.read_resource('distillery://schemas/entry-types')"
Expected: Resource distillery://schemas/entry-types registered, returns JSON with "schemas" key
Timestamp: 2026-04-09T00:00:00Z
Status: PASS

Output:
Resource URI: distillery://schemas/entry-types
Schema keys: ['bookmark', 'digest', 'feed', 'github', 'idea', 'inbox', 'meeting', 'minutes', 'person', 'project', 'reference', 'session']
session schema: {'required': {}, 'optional': {}}

Verified:
- Resource is registered at distillery://schemas/entry-types
- Content is valid JSON with top-level "schemas" key
- All EntryType variants are present in the schemas dict
- Structured types (person, project, digest, github, feed) include required/optional/constraints
