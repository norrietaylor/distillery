Type: cli
Command: distillery maintenance classify --help
Expected: Help output showing --type and --mode options
Timestamp: 2026-04-09T00:00:00Z

Output:
usage: distillery maintenance classify [-h] [--config PATH]
                                       [--format {text,json}] [--type TYPE]
                                       [--mode {llm,heuristic}]

options:
  -h, --help            show this help message and exit
  --config PATH         Path to configuration file (overrides
                        DISTILLERY_CONFIG env var and the default
                        distillery.yaml search)
  --format {text,json}  Output format (default: text)
  --type TYPE           Entry type filter for classification (default: inbox)
  --mode {llm,heuristic}
                        Classification mode: llm (LLM-based) or heuristic
                        (embedding-based). Defaults to config value.

Status: PASS
