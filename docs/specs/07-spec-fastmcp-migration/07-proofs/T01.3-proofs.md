# T01.3: Update __main__.py to use FastMCP's Built-in Runner

## Summary

Task T01.3 successfully updates the Distillery MCP server entry point to use FastMCP's built-in runner instead of the intermediate `run_server()` wrapper function. The changes simplify the startup flow by directly calling the FastMCP server's `run_stdio_async()` method from `__main__.py`.

## Changes Made

### 1. Updated `src/distillery/mcp/__main__.py`
- Changed import from `run_server` to `create_server`
- Updated `main()` function to:
  - Call `create_server()` directly to instantiate the FastMCP server
  - Call `server.run_stdio_async(show_banner=False)` instead of delegating to `run_server()`

### 2. Removed `src/distillery/mcp/server.py`
- Deleted the `run_server()` async function that was at the end of the module
- Removed the "Main entry point" section comment

## Proof Artifacts

### T01.3-01-cli.txt
- **Type**: CLI
- **Status**: PASS
- **Description**: Verified that `python -m distillery.mcp` starts without error
- **Result**: Server initializes successfully and logs "Distillery MCP server ready"

### T01.3-02-test.txt
- **Type**: Test
- **Status**: PASS
- **Description**: Verified that `create_server()` returns a FastMCP instance
- **Result**: Test `test_create_server_returns_server_instance` passes

### T01.3-03-file.txt
- **Type**: File
- **Status**: PASS
- **Description**: Verified code changes
- **Result**:
  - `run_server()` function successfully removed from `server.py`
  - `__main__.py` correctly updated to use `create_server()` and `server.run_stdio_async()`

## Functional Requirements Met

✓ Updated `__main__.py` to use FastMCP's built-in stdio runner (`server.run_stdio_async()`)
✓ Removed the `run_server()` function from `server.py`
✓ Removed unnecessary imports (no longer need to import `run_server`)
✓ Verified that `python -m distillery.mcp` starts without error
✓ Server logs "Distillery MCP server ready" indicating successful initialization
✓ `create_server()` factory function preserved for E2E tests

## Dependencies

- Task T01.2 (Implement FastMCP server scaffold) - COMPLETED
- Task T01.1 (Swap mcp dependency for fastmcp) - COMPLETED

## Blocks

- Task T01 (FastMCP Server Scaffold and Lifespan) - ready to complete
