Type: test
Description: Verify CSS specificity now allows tab visibility toggle
Expected: Tab visibility is controlled by .active class, not overridden by inline styles
Timestamp: 2026-04-02T00:10:00Z

TEST: CSS Specificity and Tab Visibility
========================================

CSS Rules in popup.css (lines 128-134):
  .tab-panel {
    display: none;
  }

  .tab-panel.active {
    display: block;
  }

JavaScript in popup.js (lines 176-187):
  tabButtons.forEach(button => {
    button.addEventListener('click', () => {
      // Remove active class from all buttons and panels
      tabButtons.forEach(btn => btn.classList.remove('active'));
      tabPanels.forEach(panel => panel.classList.remove('active'));

      // Add active class to clicked button
      button.classList.add('active');

      // Show corresponding tab panel
      const tabName = button.getAttribute('data-tab');
      const tabPanel = document.getElementById(`${tabName}-tab`);
      if (tabPanel) {
        tabPanel.classList.add('active');
      }
      ...
    });
  });

CSS Specificity Before Fix:
- Inline style (specificity: 1000): style="display: none;" ← WINS
- Class selector (specificity: 10): .tab-panel.active { display: block; }
- Result: Tab never becomes visible, even with .active class

CSS Specificity After Fix:
- No inline style
- Class selector (specificity: 10): .tab-panel.active { display: block; } ← WINS
- Result: Tab correctly displays when .active class is applied

Test Verification:
1. Watch tab has id="watch-tab" and class="tab-panel"
2. No inline style="display: none;" attribute
3. CSS will hide .tab-panel by default
4. When .active class is added by JavaScript, .tab-panel.active applies
5. Watch tab becomes visible when user clicks the Watch tab button

Status: PASS
The Watch tab is now correctly controlled by CSS classes and can be made
visible by the tab switching mechanism in popup.js.
